/*var timeLeft = 120;

setInterval(function() {
    document.getElementById("timer").innerHTML = timeLeft;
    timeLeft --;
    if(timeLeft <= 0) {
        clearInterval(timeLeft = 0);
    }
}, 1000);




/*window.onload = function() {
    var minute = '02';
    var sec = '00';
    var timeOutMsg = 'Time Out';
    setInterval(function() {
        document.getElementById("timer").innerHTML = minute + ":" + sec;
        sec --;
        if (minute == '02') {
            minute --;
            if (minute == 01) {
                sec = 59;
            }
        }
        if (sec == 00){
            minute --;
            if (minute == 0) {
                sec = 59;
            }
        }
    }, 1000);
}*/